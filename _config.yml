# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: FANAN KWANGA
email: fanankwanga.fk@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  I’m a data analyst who translates messy spreadsheet chaos into clear human stories.
  Basically, I spend my days looking at columns of numbers, 
  spotting the weird patterns everyone else misses, and using them to figure out what actually happens next. 
  I run on strong coffee, clean datasets, and the absolute thrill of an unexpected trend line. 
  Curious about my background and what drives my work? Read the full story on my About page.
baseurl: "/hitchens" # the subpath of your site, e.g. /blog
url: "https://kwanga.github.io" # the base hostname & protocol for your site, e.g. http://example.com
icon: /assets/images/icon-512.png
linkedin_username: Fanan kwanga
github_username:  Kwanga

# Build settings
markdown: kramdown

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

plugins:
  - jekyll-paginate

paginate: 3
paginate_path: "/page/:num/"